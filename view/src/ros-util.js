// const io = require("socket.io-client");
// const rosWsConnection = io(`localhost:${process.env.VUE_APP_ROS_WS_PORT}`);

// function creatTopicSubscription(topicName) {
//   rosWsConnection.on(topicName, (response) => console.log(response));
// }

// export { creatTopicSubscription };